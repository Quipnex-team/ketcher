@quipnex-team:registry=https://npm.pkg.github.com/
# Auth token comes from NODE_AUTH_TOKEN env var (CI), or write to ~/.npmrc:
#   //npm.pkg.github.com/:_authToken=YOUR_PERSONAL_ACCESS_TOKEN
# For CI/CD, this is handled automatically via the publish workflow.
